import React from "react";

const ReservationList = () => (
  <div style={{ padding: "2rem" }}>
    <h2>Reservation List</h2>
    <p>Displays all customer bookings and reservations for this theatre owner.</p>
  </div>
);

export default ReservationList;
